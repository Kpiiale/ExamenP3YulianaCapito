<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ExamenP3YulianaCapito.ViewModels"
             x:Class="ExamenP3YulianaCapito.HistorialView"
             Title="HistorialView">
    <ContentPage.BindingContext>
        <vm:HistorialViewModel />
    </ContentPage.BindingContext>

    <StackLayout Padding="20">
        <Label Text="Historial de Búsquedas" FontSize="20" HorizontalOptions="Center" />
        <ListView ItemsSource="{Binding Historial}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" Detail="{Binding City}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>
